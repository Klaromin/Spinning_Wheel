using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WheelController : BaseController<WheelModel,WheelView>
{
    public WheelController(WheelModel model, WheelView view) : base(model, view)
    {
        
    }
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }


}
